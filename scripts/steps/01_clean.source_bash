. "$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")/00_setup.source_bash"

cd ..

rm -rf run outflow work_* board_specific.* efinity_prj log.txt 

if [ -n "${openlane_dir-}" ] ; then
    rm -rf "$openlane_dir/designs/$lab_name"
fi
